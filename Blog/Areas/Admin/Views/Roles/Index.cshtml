@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "Роли";
}
<h1>@ViewData["Title"]</h1>
<table class="table table-bordered">
        <thead>
            <tr>
                <th scope="col">Роль</th>
                <th scope="col">Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>
                        <a type="button" class="btn btn-danger" asp-controller="Roles" asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                    </td>
                </tr>
            }
        </tbody>
</table>
<br />
<a type="button" class="btn btn-primary" asp-controller="Roles" asp-action="Create">Добавить роль</a>
